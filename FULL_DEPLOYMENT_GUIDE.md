# 📋 Полная инструкция по развертыванию MOEX Trading Bot на ВМ

## 🎯 Цель
Развернуть торгового бота на виртуальной машине с настройкой:
- Sandbox модель: только обучение (сбор данных, без торговли)
- Live модель: реальная торговля через Finam API
- Один токен и один счет Finam для обеих моделей

---

## 📝 Шаг 1: Подключение к ВМ

На вашем локальном компьютере (PowerShell):

```powershell
ssh ubuntu@<IP_вашей_ВМ>
# или
ssh admin@<IP_вашей_ВМ>
```

**Пример:**
```powershell
ssh admin@89.208.198.45
# Введите пароль при запросе
```

---

## 🧹 Шаг 2: Очистка ВМ от старых данных

Выполните на ВМ:

```bash
# 1. Остановка бота (если запущен)
cd ~/moex_trading_bot 2>/dev/null && ./deploy/stop_bot.sh || true

# 2. Удаление старой директории проекта
rm -rf ~/moex_trading_bot

# 3. Очистка процессов Python (если остались)
pkill -f "python.*main.py" || true

# 4. Проверка, что директория удалена
ls -la ~ | grep moex_trading_bot
# (ничего не должно вывестись)
```

---

## 📥 Шаг 3: Клонирование проекта из GitHub

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone https://github.com/Egor123458/moex-trading-bot.git moex_trading_bot

# Переход в директорию проекта
cd moex_trading_bot

# Проверка, что файлы склонированы
ls -la
```

**Ожидаемый результат:** Должны быть видны файлы `main.py`, `requirements.txt`, `config/`, `src/` и т.д.

---

## 🔧 Шаг 4: Установка системных зависимостей

```bash
# Обновление списка пакетов
sudo apt update

# Установка Python, pip, venv и git
sudo apt install -y python3 python3-pip python3-venv git

# Проверка версии Python (должна быть 3.8+)
python3 --version
```

**Ожидаемый результат:** `Python 3.8.x` или выше

---

## 🐍 Шаг 5: Создание виртуального окружения Python

```bash
# Убедитесь, что вы в директории проекта
cd ~/moex_trading_bot

# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Проверка, что venv активирован (в начале строки должно быть (venv))
which python
# Должен показать: /home/ubuntu/moex_trading_bot/venv/bin/python

# Обновление pip до последней версии
pip install --upgrade pip
```

**Ожидаемый результат:** В начале командной строки должно появиться `(venv)`

---

## 📦 Шаг 6: Установка Python зависимостей

```bash
# Убедитесь, что venv активирован (должно быть (venv) в начале строки)
# Если нет, выполните: source venv/bin/activate

# Установка всех зависимостей из requirements.txt
pip install -r requirements.txt

# Это может занять несколько минут. Дождитесь завершения.

# Проверка установки основных пакетов
pip list | grep -E "requests|pandas|sqlalchemy|dotenv"
```

**Ожидаемый результат:** Должны быть установлены `requests`, `pandas`, `sqlalchemy`, `python-dotenv`

---

## 📁 Шаг 7: Создание необходимых директорий

```bash
# Создание директорий для логов и данных
mkdir -p logs data

# Проверка создания директорий
ls -la | grep -E "logs|data"
```

**Ожидаемый результат:** Должны быть видны директории `logs/` и `data/`

---

## ⚙️ Шаг 8: Настройка .env файла

```bash
# Создание .env файла
nano .env
```

**В редакторе nano вставьте следующее содержимое:**

```bash
# Тип брокера
BROKER_TYPE=finam

# Режим работы (dual_mode = обе модели одновременно)
MODE=dual_mode

# Finam токен (один для обеих моделей)
FINAM_TOKEN=TXBO:01KEHQJ874S7M8XKNMY95KPFD7

# Счет Finam (один для обеих моделей)
FINAM_ACCOUNT_ID=КлФ-2012789
FINAM_SANDBOX_ACCOUNT_ID=КлФ-2012789
FINAM_LIVE_ACCOUNT_ID=КлФ-2012789

# Режим sandbox: только обучение (без торговли)
# true = sandbox только собирает данные и учится, не торгует
# false = sandbox торгует (как и live)
SANDBOX_LEARNING_ONLY=true

# Общие настройки
INITIAL_CAPITAL=1000000
DATABASE_URL=sqlite:///data/trading_bot.db

# Логирование
LOG_LEVEL=INFO

# Telegram (опционально, если используете)
# TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
# TELEGRAM_CHAT_ID=ваш_chat_id
```

**Сохранение в nano:**
1. Нажмите `Ctrl+O` (сохранить)
2. Нажмите `Enter` (подтвердить имя файла)
3. Нажмите `Ctrl+X` (выйти)

**Проверка содержимого .env:**
```bash
cat .env
```

**Ожидаемый результат:** Должен показать все настройки, которые вы вставили

---

## 🧪 Шаг 9: Тестовый запуск

```bash
# Убедитесь, что venv активирован
source venv/bin/activate

# Тестовый запуск бота
python main.py
```

**Ожидаемый вывод в консоли:**

```
============================================================
ИНИЦИАЛИЗАЦИЯ ТОРГОВОГО БОТА
============================================================
Тип брокера: finam
Режим: dual_mode
✅ Sandbox Finam клиент инициализирован
✅ Live Finam клиент инициализирован
✅ Подключение к БД установлено
✅ MOEX API инициализирован
============================================================
ЗАПУСК ТОРГОВОГО БОТА MOEX AI
Режим: dual_mode
Начальный капитал: 1,000,000 ₽
============================================================
✅ Бот запущен и работает
Торговый цикл будет выполняться каждые 30 минут

============================================================
ТОРГОВЫЙ ЦИКЛ #1
============================================================
--- Торговый цикл (2026-01-XX XX:XX:XX) ---
--- Sandbox цикл обучения (без торговли) ---
Сбор данных для обучения...
--- Live торговый цикл ---
```

**Если видите эти сообщения — все работает!**

**Остановка тестового запуска:**
- Нажмите `Ctrl+C`

---

## 🚀 Шаг 10: Запуск в фоновом режиме

```bash
# Убедитесь, что вы в директории проекта
cd ~/moex_trading_bot

# Убедитесь, что venv активирован
source venv/bin/activate

# Сделать скрипт запуска исполняемым
chmod +x deploy/run_bot.sh

# Запуск бота в фоне
./deploy/run_bot.sh
```

**Или вручную:**

```bash
# Запуск в фоне с сохранением логов
nohup python main.py > logs/bot.log 2>&1 &

# Сохранение PID процесса
echo $! > bot.pid

# Проверка, что бот запущен
ps aux | grep python | grep main.py
```

**Ожидаемый результат:** Должен показать процесс Python с `main.py`

---

## ✅ Шаг 11: Проверка работы

```bash
# Просмотр логов в реальном времени
tail -f logs/bot.log

# Или просмотр последних 50 строк
tail -n 50 logs/bot.log

# Проверка статуса процесса
ps aux | grep python | grep main.py
```

**Что должно быть в логах:**

```
✅ Sandbox Finam клиент инициализирован
✅ Live Finam клиент инициализирован
✅ Подключение к БД установлено
✅ MOEX API инициализирован
--- Sandbox цикл обучения (без торговли) ---
Сбор данных для обучения...
✓ Собрано X свечей для SBER
✓ Собрано X свечей для GAZP
...
Данные собраны и сохранены в БД
✓ Sandbox цикл обучения завершен (торговля отключена)
--- Live торговый цикл ---
Live цикл завершен
```

**Остановка просмотра логов:** Нажмите `Ctrl+C`

---

## 🎮 Управление ботом

### Остановка бота

```bash
# Использование скрипта
cd ~/moex_trading_bot
./deploy/stop_bot.sh

# Или вручную (если знаете PID)
kill $(cat bot.pid)
```

### Перезапуск бота

```bash
cd ~/moex_trading_bot
./deploy/stop_bot.sh
source venv/bin/activate
./deploy/run_bot.sh
```

### Проверка статуса

```bash
# Проверка процесса
ps aux | grep python | grep main.py

# Проверка логов
tail -n 20 logs/bot.log
```

---

## 🔄 Обновление проекта

Если нужно обновить код из GitHub:

```bash
cd ~/moex_trading_bot

# Остановка бота
./deploy/stop_bot.sh

# Обновление кода
git pull origin main

# Активация venv
source venv/bin/activate

# Обновление зависимостей (если изменились)
pip install -r requirements.txt

# Запуск бота
./deploy/run_bot.sh
```

---

## 🐛 Устранение проблем

### Бот не запускается

```bash
# 1. Проверка логов
tail -n 100 logs/bot.log

# 2. Проверка .env файла
cat .env

# 3. Проверка виртуального окружения
which python
# Должен показать: /home/ubuntu/moex_trading_bot/venv/bin/python

# 4. Проверка зависимостей
pip list | grep requests
```

### Ошибка "Module not found"

```bash
# Активация venv
source venv/bin/activate

# Переустановка зависимостей
pip install -r requirements.txt
```

### Ошибка подключения к Finam API

```bash
# Проверка токена в .env
grep FINAM_TOKEN .env

# Проверка номера счета
grep FINAM_ACCOUNT_ID .env
```

### Бот не собирает данные

```bash
# Проверка подключения к MOEX API
python3 -c "from src.data_collection.moex_api import MOEXDataCollector; m = MOEXDataCollector(); print('OK' if m.test_connection() else 'FAIL')"

# Проверка БД
ls -la data/
```

---

## 📊 Мониторинг работы

### Просмотр логов

```bash
# Последние 100 строк
tail -n 100 logs/bot.log

# В реальном времени
tail -f logs/bot.log

# Поиск ошибок
grep -i error logs/bot.log
```

### Проверка данных в БД

```bash
# Если используется SQLite
sqlite3 data/trading_bot.db "SELECT COUNT(*) FROM candles;"
```

---

## 📝 Краткая справка команд

```bash
# Переход в директорию проекта
cd ~/moex_trading_bot

# Активация виртуального окружения
source venv/bin/activate

# Запуск бота
./deploy/run_bot.sh

# Остановка бота
./deploy/stop_bot.sh

# Просмотр логов
tail -f logs/bot.log

# Проверка статуса
ps aux | grep python | grep main.py
```

---

## ✅ Чеклист развертывания

- [ ] Подключился к ВМ
- [ ] Очистил старые данные
- [ ] Склонировал проект из GitHub
- [ ] Установил системные зависимости
- [ ] Создал виртуальное окружение
- [ ] Установил Python зависимости
- [ ] Создал директории logs/ и data/
- [ ] Настроил .env файл с токеном и счетом
- [ ] Протестировал запуск (python main.py)
- [ ] Запустил бота в фоне
- [ ] Проверил логи (tail -f logs/bot.log)
- [ ] Убедился, что sandbox только учится, а live торгует

---

## 🎉 Готово!

Бот должен работать 24/7 на ВМ. Sandbox модель собирает данные и учится, а live модель выполняет реальную торговлю через Finam API.

**Важно:**
- Sandbox модель НЕ торгует (только обучение)
- Live модель торгует реально
- Оба используют один токен и один счет Finam
- Данные сохраняются в SQLite БД (`data/trading_bot.db`)

---

**Если возникли вопросы или проблемы — проверьте логи: `tail -f logs/bot.log`**

