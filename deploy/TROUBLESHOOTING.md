# 🔧 Устранение проблем с подключением

## Проблема: ВМ недоступна по SSH

Если `Test-NetConnection` показывает, что порт 22 недоступен, проверьте:

### 1. Проверка статуса ВМ

В панели VK Cloud:
1. Зайдите в раздел "Виртуальные машины"
2. Найдите `moex-trading-vm`
3. Убедитесь, что ВМ **запущена** (статус "Running")
4. Если остановлена - запустите её

### 2. Проверка групп безопасности (Firewall)

В панели VK Cloud:
1. Найдите вашу ВМ `moex-trading-vm`
2. Перейдите в "Группы безопасности"
3. Убедитесь, что группа `default` имеет правило:
   - **Тип:** Входящий трафик
   - **Протокол:** TCP
   - **Порт:** 22
   - **Источник:** 0.0.0.0/0 (или ваш IP)

**Если правила нет - добавьте:**
1. Нажмите "Добавить правило"
2. Выберите:
   - Направление: Входящий
   - Протокол: TCP
   - Порт: 22
   - Источник: 0.0.0.0/0 (для доступа откуда угодно) или ваш IP
3. Сохраните

### 3. Использование веб-консоли VK Cloud

Если SSH недоступен, используйте веб-консоль:

1. В панели VK Cloud найдите вашу ВМ
2. Нажмите "Подключиться" → "Веб-консоль"
3. Войдите с пользователем `ubuntu`
4. Если пароль не знаете - сбросьте его в настройках ВМ

### 4. Создание нового ключа через веб-консоль

Если подключились через веб-консоль:

```bash
# Создайте новый ключ на ВМ
ssh-keygen -t rsa -b 4096 -f ~/.ssh/new_key -N ""

# Покажите публичный ключ
cat ~/.ssh/new_key.pub

# Добавьте его в authorized_keys
cat ~/.ssh/new_key.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Затем скачайте приватный ключ и используйте его для подключения.

### 5. Альтернативный способ: Создать новую ключевую пару в VK Cloud

1. В панели VK Cloud перейдите в "Ключевые пары"
2. Создайте новую ключевую пару
3. **Скачайте приватный ключ** (он скачается только один раз!)
4. Сохраните его в безопасном месте
5. Привяжите ключ к ВМ в настройках

### 6. Проверка через другой компьютер/сеть

Возможно, ваш провайдер блокирует порт 22. Попробуйте:
- С другого компьютера
- Через VPN
- С мобильного интернета (телефон в режиме точки доступа)

## Быстрое решение: Веб-консоль + развертывание

Если SSH не работает, используйте веб-консоль:

1. **Подключитесь через веб-консоль VK Cloud**
2. **Выполните команды напрямую:**

```bash
# Обновление системы
sudo apt-get update && sudo apt-get upgrade -y

# Установка git
sudo apt-get install -y git

# Клонирование репозитория
cd /tmp
git clone https://github.com/Egor123458/moex-trading-bot.git
cd moex-trading-bot

# Запуск развертывания
chmod +x deploy/deploy.sh
sudo ./deploy/deploy.sh
```

## Проверка после настройки firewall

После добавления правила в группу безопасности:

```powershell
# Подождите 1-2 минуты, затем проверьте снова
Test-NetConnection -ComputerName 89.208.197.34 -Port 22

# Если доступен - подключайтесь
ssh -i путь/к/ключу.pem ubuntu@89.208.197.34
```

## Контакты поддержки

Если ничего не помогает:
1. Проверьте документацию VK Cloud
2. Обратитесь в поддержку VK Cloud
3. Убедитесь, что у вас есть доступ к панели управления

