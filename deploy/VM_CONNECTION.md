# 🔌 Подключение к правильной ВМ

## ⚠️ Важно: Разница между ВМ

У вас есть **4 виртуальные машины**:

### 1. ВМ приложения (для развертывания бота) ✅
- **Имя:** `moex-trading-vm`
- **IP:** `89.208.196.98` (внешний)
- **Пользователь:** `admin` или `ubuntu`
- **Назначение:** Здесь нужно развернуть торгового бота

### 2-4. ВМ базы данных (только для БД) ❌
- **Имена:** `moex-trading-db-member-0`, `member-1`, `member-2`
- **IP:** `10.0.0.129`, `10.0.0.249`, `10.0.0.73` (внутренние)
- **Пользователь:** `admin` (ограниченные права)
- **Назначение:** Только для PostgreSQL кластера

## ✅ Правильная команда подключения

**Для развертывания бота подключайтесь к ВМ приложения:**

```powershell
ssh -i "C:\Users\Egor Galkin\Downloads\moex-trading-vm-0x5ulVyq.pem" admin@89.208.196.98
```

Или если пользователь `ubuntu`:
```powershell
ssh -i "C:\Users\Egor Galkin\Downloads\moex-trading-vm-0x5ulVyq.pem" ubuntu@89.208.196.98
```

## 🔍 Как определить, к какой ВМ вы подключены

После подключения проверьте:

```bash
# Проверка имени хоста
hostname

# Должно быть: moex-trading-vm или что-то похожее
# НЕ должно быть: moex-trading-db-member-*
```

Или проверьте IP:
```bash
# Проверка внешнего IP
curl ifconfig.me

# Должно быть: 89.208.196.98
```

## 📋 Пошаговая инструкция

### Шаг 1: Отключитесь от текущей ВМ БД

```bash
exit
```

### Шаг 2: Подключитесь к ВМ приложения

**На вашем локальном компьютере (PowerShell):**

```powershell
ssh -i "C:\Users\Egor Galkin\Downloads\moex-trading-vm-0x5ulVyq.pem" admin@89.208.196.98
```

### Шаг 3: Проверьте, что это правильная ВМ

```bash
hostname
# Должно показать что-то вроде: moex-trading-vm

# Проверка внешнего IP
curl ifconfig.me
# Должно показать: 89.208.196.98
```

### Шаг 4: Теперь можно развертывать

```bash
sudo bash

cd /tmp
git clone https://github.com/Egor123458/moex-trading-bot.git
cd moex-trading-bot
chmod +x deploy/deploy.sh
./deploy/deploy.sh
```

## ❌ Если вы на ВМ БД

Если вы случайно подключились к ВМ БД (`moex-trading-db-member-0`), вы увидите:
- Ограниченные права (не можете выполнить `sudo bash`)
- Имя хоста содержит `db-member`
- Внутренний IP (10.0.0.x)

**В этом случае:**
1. Отключитесь: `exit`
2. Подключитесь к правильной ВМ (см. выше)

## 📝 Резюме

- **ВМ приложения** (`89.208.196.98`) - для развертывания бота ✅
- **ВМ БД** (`10.0.0.129/249/73`) - только для базы данных ❌

