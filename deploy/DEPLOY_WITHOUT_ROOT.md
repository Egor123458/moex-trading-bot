# 🚀 Развертывание БЕЗ прав администратора

## ✅ Преимущества

- Не требуются права root/sudo
- Все файлы в домашней директории пользователя
- Проще управлять и обновлять
- Безопаснее (нет системных изменений)

## 📋 Пошаговая инструкция

### Шаг 1: Подключение к ВМ

```powershell
# Через SSH (если настроен)
ssh -i "C:\Users\Egor Galkin\Downloads\moex-trading-vm-0x5ulVyq.pem" admin@89.208.198.45

# Или через веб-консоль VK Cloud
# Пользователь: admin, Пароль: kT9mL2pQ
```

### Шаг 2: Развертывание

```bash
# Клонирование репозитория
cd ~
git clone https://github.com/Egor123458/moex-trading-bot.git moex_trading_bot
cd moex_trading_bot

# Запуск скрипта развертывания (без root)
chmod +x deploy/deploy_user.sh
./deploy/deploy_user.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение Python
- ✅ Установит все зависимости
- ✅ Создаст необходимые директории
- ✅ Подготовит .env файл

### Шаг 3: Настройка .env

```bash
nano ~/moex_trading_bot/.env
```

Укажите обязательные параметры:
```env
# API ключи
TINKOFF_TOKEN=ваш_токен
TINKOFF_ACCOUNT_ID=ваш_account_id

# База данных (внутренний IP Master БД)
DATABASE_URL=postgresql://admin:Admin123456@10.0.0.129:5432/trading_bot

# Режим работы
MODE=paper_trading

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=ваш_bot_token
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Шаг 4: Запуск бота

#### Вариант A: Запуск в текущей сессии (для тестирования)

```bash
cd ~/moex_trading_bot
source venv/bin/activate
python main.py
```

#### Вариант B: Запуск в фоновом режиме (рекомендуется)

```bash
cd ~/moex_trading_bot
chmod +x deploy/run_bot.sh
./deploy/run_bot.sh
```

Бот запустится в фоне, логи будут в `~/moex_trading_bot/logs/bot.log`

#### Вариант C: Использование screen (для постоянной работы)

```bash
# Установка screen (если нет)
# sudo apt-get install screen  # или попросите админа установить

# Создание сессии
screen -S trading_bot

# Запуск бота
cd ~/moex_trading_bot
source venv/bin/activate
python main.py

# Отключение от сессии: Ctrl+A, затем D
# Подключение к сессии: screen -r trading_bot
```

#### Вариант D: Использование tmux

```bash
# Установка tmux (если нет)
# sudo apt-get install tmux

# Создание сессии
tmux new -s trading_bot

# Запуск бота
cd ~/moex_trading_bot
source venv/bin/activate
python main.py

# Отключение: Ctrl+B, затем D
# Подключение: tmux attach -t trading_bot
```

## 🔧 Управление ботом

### Проверка статуса

```bash
cd ~/moex_trading_bot
./deploy/status_bot.sh
```

### Остановка бота

```bash
cd ~/moex_trading_bot
./deploy/stop_bot.sh
```

### Просмотр логов

```bash
# Логи бота
tail -f ~/moex_trading_bot/logs/bot.log

# Логи торговли
tail -f ~/moex_trading_bot/logs/trading/*.log

# Логи ошибок
tail -f ~/moex_trading_bot/logs/errors/*.log
```

## 🔄 Обновление бота

```bash
cd ~/moex_trading_bot

# Остановка бота
./deploy/stop_bot.sh

# Обновление кода
git pull origin main

# Обновление зависимостей
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запуск бота
./deploy/run_bot.sh
```

## ⚙️ Автозапуск при перезагрузке (без systemd)

### Вариант 1: Добавить в .bashrc (запуск при входе)

```bash
nano ~/.bashrc
```

Добавьте в конец файла:
```bash
# Автозапуск торгового бота
if [ -f ~/moex_trading_bot/deploy/run_bot.sh ]; then
    cd ~/moex_trading_bot
    if [ ! -f bot.pid ] || ! ps -p $(cat bot.pid) > /dev/null 2>&1; then
        ./deploy/run_bot.sh
    fi
fi
```

### Вариант 2: Использование cron (рекомендуется)

```bash
# Редактирование crontab
crontab -e

# Добавьте строку для автозапуска при перезагрузке
@reboot sleep 60 && cd ~/moex_trading_bot && ./deploy/run_bot.sh >> ~/moex_trading_bot/logs/cron.log 2>&1
```

## 📊 Мониторинг

### Проверка процессов

```bash
# Поиск процесса бота
ps aux | grep "python.*main.py"

# Использование ресурсов
top -p $(cat ~/moex_trading_bot/bot.pid)
```

### Проверка подключения к БД

```bash
# С ВМ приложения
psql -h 10.0.0.129 -U admin -d trading_bot -c "SELECT COUNT(*) FROM candles;"
# Пароль: Admin123456
```

## 🔍 Устранение проблем

### Бот не запускается

```bash
# Проверка логов
tail -n 50 ~/moex_trading_bot/logs/bot.log

# Проверка .env файла
cat ~/moex_trading_bot/.env

# Проверка виртуального окружения
cd ~/moex_trading_bot
source venv/bin/activate
python --version
pip list
```

### Проблемы с зависимостями

```bash
cd ~/moex_trading_bot
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### Проблемы с подключением к БД

```bash
# Проверка доступности БД
ping 10.0.0.129

# Проверка порта
telnet 10.0.0.129 5432

# Тестовое подключение
psql -h 10.0.0.129 -U admin -d postgres
```

## 📝 Структура файлов

После развертывания структура будет такой:

```
~/moex_trading_bot/
├── venv/              # Виртуальное окружение
├── data/              # Данные
│   ├── raw/
│   ├── processed/
│   ├── models/
│   └── backtest/
├── logs/              # Логи
│   ├── trading/
│   ├── errors/
│   └── performance/
├── .env               # Переменные окружения
├── bot.pid            # PID процесса (если запущен)
└── main.py            # Главный файл
```

## ✅ Преимущества этого подхода

1. **Не требует root прав** - все в домашней директории
2. **Простое управление** - простые скрипты запуска/остановки
3. **Легкое обновление** - git pull и перезапуск
4. **Безопасность** - нет системных изменений
5. **Гибкость** - можно использовать screen/tmux для сессий

## 🆚 Сравнение с systemd версией

| Параметр | Без root | С systemd |
|----------|----------|-----------|
| Требует root | ❌ Нет | ✅ Да |
| Автозапуск | ⚠️ Через cron | ✅ Нативный |
| Управление | Скрипты | systemctl |
| Логи | Файлы | journalctl |
| Перезапуск при сбое | ❌ Нет | ✅ Да |

**Рекомендация:** Если есть возможность получить root права - используйте systemd версию. Если нет - этот вариант отлично подойдет!

