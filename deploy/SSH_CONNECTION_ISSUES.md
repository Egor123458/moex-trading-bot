# 🔧 Устранение проблем с SSH подключением

## ❌ Ошибка: "Connection timed out"

Если вы получаете ошибку:
```
ssh: connect to host 89.208.198.45 port 22: Connection timed out
```

Это означает, что порт 22 недоступен. Вот что нужно проверить:

---

## ✅ Решение 1: Проверка статуса ВМ

### В панели VK Cloud:

1. Зайдите в панель управления: https://msk.cloud.vk.com
2. Перейдите в раздел **"Виртуальные машины"**
3. Найдите ВМ `moex_trading_app-member-0` (ID: c8b7d9ca-f5g7-5h8i-c747-01f657b895e8)
4. **Убедитесь, что ВМ запущена** (статус должен быть "Running")
5. Если ВМ остановлена - **запустите её**

---

## ✅ Решение 2: Проверка групп безопасности (Firewall)

### В панели VK Cloud:

1. Найдите вашу ВМ `moex_trading_app-member-0`
2. Перейдите в раздел **"Группы безопасности"** или **"Firewall"**
3. Проверьте, что есть правило для SSH:
   - **Направление:** Входящий трафик (Inbound)
   - **Протокол:** TCP
   - **Порт:** 22
   - **Источник:** `0.0.0.0/0` (для доступа откуда угодно) или ваш IP

### Если правила нет - добавьте:

1. Нажмите **"Добавить правило"** или **"Edit Security Groups"**
2. Создайте правило:
   ```
   Type: SSH
   Protocol: TCP
   Port: 22
   Source: 0.0.0.0/0
   ```
3. **Сохраните изменения**
4. Подождите 1-2 минуты для применения правил

---

## ✅ Решение 3: Использование веб-консоли VK Cloud

**Если SSH недоступен, используйте веб-консоль:**

1. В панели VK Cloud найдите ВМ `moex_trading_app-member-0`
2. Нажмите кнопку **"Подключиться"** → **"Веб-консоль"** или **"VNC Console"**
3. Войдите с пользователем `admin` и паролем `kT9mL2pQ`
4. Выполните развертывание прямо в веб-консоли:

```bash
# Получение root прав
sudo -i
# Пароль: kT9mL2pQ

# Клонирование репозитория
cd /tmp
git clone https://github.com/Egor123458/moex-trading-bot.git
cd moex-trading-bot

# Запуск развертывания
chmod +x deploy/deploy.sh
./deploy/deploy.sh
```

---

## ✅ Решение 4: Проверка вашего IP и firewall

### Возможные причины блокировки:

1. **Ваш провайдер блокирует порт 22**
   - Попробуйте с другого компьютера
   - Используйте VPN
   - Попробуйте с мобильного интернета (телефон в режиме точки доступа)

2. **Ваш локальный firewall блокирует**
   ```powershell
   # Проверка Windows Firewall
   Get-NetFirewallRule | Where-Object {$_.DisplayName -like "*SSH*"}
   ```

3. **Проверка доступности с вашего компьютера**
   ```powershell
   # Проверка доступности
   Test-NetConnection -ComputerName 89.208.198.45 -Port 22
   
   # Ping проверка
   ping 89.208.198.45
   ```

---

## ✅ Решение 5: Альтернативные способы подключения

### Вариант A: Через другие ВМ

Если у вас есть доступ к другим ВМ (например, БД), подключитесь через них:

```bash
# С ВМ БД (89.208.196.98)
ssh admin@10.0.0.131  # Внутренний IP ВМ приложения
```

### Вариант B: Изменение порта SSH (если возможно)

Если у вас есть доступ к веб-консоли, можно изменить порт SSH:

```bash
# На ВМ приложения через веб-консоль
sudo nano /etc/ssh/sshd_config
# Измените Port 22 на другой порт (например, 2222)
sudo systemctl restart sshd
```

Затем добавьте правило firewall для нового порта.

---

## 🔍 Диагностика

### Проверка доступности ВМ:

```powershell
# PowerShell
Test-NetConnection -ComputerName 89.208.198.45 -Port 22
Test-NetConnection -ComputerName 89.208.198.45 -Port 80
ping 89.208.198.45
```

### Проверка через веб-браузер:

Попробуйте открыть в браузере (если есть веб-сервер):
```
http://89.208.198.45
```

---

## 📋 Чек-лист перед подключением

- [ ] ВМ запущена (статус "Running")
- [ ] Группа безопасности имеет правило для порта 22
- [ ] Правило разрешает входящий трафик с вашего IP или 0.0.0.0/0
- [ ] ВМ имеет назначенный внешний IP (89.208.198.45)
- [ ] Ваш провайдер не блокирует порт 22
- [ ] Локальный firewall не блокирует SSH

---

## 🚀 Рекомендуемое решение

**Если SSH не работает, используйте веб-консоль VK Cloud:**

1. Зайдите в панель VK Cloud
2. Найдите ВМ `moex_trading_app-member-0`
3. Нажмите "Подключиться" → "Веб-консоль"
4. Войдите: `admin` / `kT9mL2pQ`
5. Выполните развертывание (см. START_HERE.md)

Это самый быстрый способ начать работу, пока не настроен SSH доступ.

---

## 📞 Дополнительная помощь

Если ничего не помогает:
1. Проверьте документацию VK Cloud по настройке firewall
2. Обратитесь в поддержку VK Cloud
3. Убедитесь, что у вас есть права администратора в проекте

