# Инструкция по чистому развертыванию на ВМ

## Шаг 1: Очистка ВМ от старых данных

Выполните на ВМ:

```bash
# 1. Остановка бота (если запущен)
cd ~/moex_trading_bot 2>/dev/null && ./deploy/stop_bot.sh || true

# 2. Удаление старой директории
rm -rf ~/moex_trading_bot

# 3. Очистка процессов (если остались)
pkill -f "python.*main.py" || true
```

## Шаг 2: Клонирование чистого проекта

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone https://github.com/Egor123458/moex-trading-bot.git moex_trading_bot

# Переход в директорию проекта
cd moex_trading_bot
```

## Шаг 3: Установка системных зависимостей

```bash
# Обновление списка пакетов
sudo apt update

# Установка Python и необходимых инструментов
sudo apt install -y python3 python3-pip python3-venv git
```

## Шаг 4: Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Обновление pip
pip install --upgrade pip
```

## Шаг 5: Установка Python зависимостей

```bash
# Установка всех зависимостей из requirements.txt
pip install -r requirements.txt
```

## Шаг 6: Создание необходимых директорий

```bash
# Создание директорий для логов и данных
mkdir -p logs data
```

## Шаг 7: Настройка .env файла

```bash
# Создание .env файла
nano .env
```

Вставьте следующее содержимое:

```bash
# Тип брокера
BROKER_TYPE=finam

# Режим работы (dual_mode = обе модели одновременно)
MODE=dual_mode

# Finam токен (один для обеих моделей)
FINAM_TOKEN=TXBO:01KEHQJ874S7M8XKNMY95KPFD7

# Счет Finam (один для обеих моделей)
FINAM_ACCOUNT_ID=КлФ-2012789
FINAM_SANDBOX_ACCOUNT_ID=КлФ-2012789
FINAM_LIVE_ACCOUNT_ID=КлФ-2012789

# Режим sandbox: только обучение (без торговли)
SANDBOX_LEARNING_ONLY=true

# Общие настройки
INITIAL_CAPITAL=1000000
DATABASE_URL=sqlite:///data/trading_bot.db

# Логирование
LOG_LEVEL=INFO
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

## Шаг 8: Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Тестовый запуск
python main.py
```

Ожидаемый вывод:
- `✅ Sandbox Finam клиент инициализирован`
- `✅ Live Finam клиент инициализирован`
- `✅ Подключение к БД установлено`
- `✅ MOEX API инициализирован`
- `--- Sandbox цикл обучения (без торговли) ---`
- `--- Live торговый цикл ---`

Остановите тестовый запуск: `Ctrl+C`

## Шаг 9: Запуск в фоновом режиме

```bash
# Использование готового скрипта
chmod +x deploy/run_bot.sh
./deploy/run_bot.sh
```

Или вручную:

```bash
# Запуск в фоне
nohup python main.py > logs/bot.log 2>&1 &

# Проверка, что бот запущен
ps aux | grep python
```

## Шаг 10: Проверка работы

```bash
# Просмотр логов в реальном времени
tail -f logs/bot.log

# Или просмотр последних 50 строк
tail -n 50 logs/bot.log
```

## Управление ботом

```bash
# Остановка бота
./deploy/stop_bot.sh

# Перезапуск
./deploy/stop_bot.sh
./deploy/run_bot.sh

# Проверка статуса
ps aux | grep python
```

## Что делает каждая модель

### Sandbox модель (обучение):
- ✅ Собирает данные с MOEX API
- ✅ Сохраняет данные в БД
- ✅ Генерирует сигналы для анализа
- ❌ НЕ размещает ордера (торговля отключена)

### Live модель (торговля):
- ✅ Получает портфель через Finam API
- ✅ Генерирует сигналы
- ✅ Размещает ордера через Finam API
- ✅ Выполняет реальную торговлю

## Если возникли ошибки

1. Проверьте .env файл: `cat .env`
2. Проверьте логи: `tail -f logs/bot.log`
3. Проверьте, что виртуальное окружение активировано: `which python` (должен показать путь к venv)
4. Проверьте установку зависимостей: `pip list | grep requests`

## Обновление проекта

Если нужно обновить код:

```bash
cd ~/moex_trading_bot
./deploy/stop_bot.sh
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
./deploy/run_bot.sh
```

---

**Готово! Бот должен работать 24/7 на ВМ** 🎉

